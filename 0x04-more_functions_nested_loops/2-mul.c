#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers.
 * @a: parameter to be used
 * @b: second parameter to be used
 * Return: 0
 */
int mul(int a, int b)
{
	int results;

	results = a * b;

	return (results);
}
