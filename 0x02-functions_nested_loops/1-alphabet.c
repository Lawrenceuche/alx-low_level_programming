#include <stdio.h>
#include "main.h"
/**
 * main - prints the alphabet, in lowercase,
 * followed by a new line.
 * Return: 0
 */
void print_alphabet(void);
int main(void)
{
	char c;

	for (c = 'a'; c <= 'a'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
